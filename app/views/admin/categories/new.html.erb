<% content_for :title, "New Category" %>

<div class="w-full px-4 sm:px-6 lg:px-8 py-8">
  <div class="max-w-3xl mx-auto bg-white rounded-xl shadow-md border border-gray-200 p-6">
    <h1 class="text-2xl sm:text-3xl font-bold text-gray-800 mb-6">New Category</h1>

    <%= render "form", admin_category: @admin_category %>

    <div class="mt-6">
      <%= link_to "← Back to Categories", admin_categories_path,
          class: "inline-block text-center rounded-md px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-700 font-medium" %>
    </div>
  </div>
</div>
